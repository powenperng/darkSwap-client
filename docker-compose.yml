version: '3'
services:
  app:
    container_name: singularity-pro
    image: singularitynetwork/singularity-pro
    build: .
    ports:
      - "3002:3002"
    volumes:
      - ./data:/data
    environment:
      - NODE_ENV=prodution

